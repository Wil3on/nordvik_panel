/**
 * Utility functions for handling multipart form data in Next.js API routes
 */

/**
 * Parse multipart form data from a request
 * This is a placeholder for now - Next.js handles formData parsing internally
 */
export async function parseMultipartFormData(request: Request): Promise<FormData> {
  return request.formData();
}
